import React from 'react'

import styles from '~/assets/css/button/savebutton.module.css'

const SaveButton = () => {
  return (
    <button className={styles.button} type="submit">
      Save
    </button>
  )
}

export default SaveButton
